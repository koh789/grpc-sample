syntax = "proto3";
// https://grpc.io/docs/languages/go/quickstart/
option go_package="pkg/grpc";
package pinger;

service Pinger {
    rpc Ping(Empty) returns (Pong) {}
}

message Empty {}

message Pong {
    string text = 1;
}